// Copy to ios/Secrets.xcconfig and fill in. That copy is gitignored.
// The verify gates create it from this file when it is missing.
//
// GitHub's device flow needs a client ID and no client secret, so this
// value is not a credential. Leave it empty and set the ID inside the app
// under Settings instead.
GITHUB_CLIENT_ID =
